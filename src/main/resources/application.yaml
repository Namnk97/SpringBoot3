server:
  port: 8888
  servlet:
    context-path: /Booking

spring:
  datasource:
    url: "jdbc:mysql://localhost:3333/Booking_Online"
    username: root
    password: root

jpa:
  hibernate:
    ddl-auto: create
  show-sql: true

